# 2026-09-01

## Préparation de la validation visuelle : `wip-demo-jouable` resynchronisée

Nouveau répertoire de travail : `~/GoogleDrive2/Echecs/Papu-Chess`
(synchronisé Drive), à la place de `~/Téléchargements/Echecs/Papu-Chess`.

### 1. Le retard était double

La consigne annonçait la branche en retard de trois commits sur `main`
(`fec7ee1`, `416d4ef`, `def245c`). C'est vrai — mais `git log HEAD..main`
sortait **vide**, parce que le `main` **local** portait le même retard
(il était resté à `106b72d`). Le retard était sur `origin/main`.

Geste en deux temps, pour que la fusion porte bien les trois commits :
avance rapide de `main` local sur `origin/main`
(`git fetch origin main:main`, `106b72d..fec7ee1`), puis
`git merge --no-ff main` depuis `wip-demo-jouable`.

**Aucun conflit.** Quatre fichiers entrent :`.gitignore`, `CLAUDE.md`,
`REPRISE.md`, `_rapports/.gitkeep` — 54 insertions, 18 suppressions.
`REPRISE.md` a fusionné automatiquement. **Aucun fichier du chantier
« démonstration jouable » n'est touché** : ni `Papu_Chess.html`, ni
`FORMAT.md`, ni `echec-a-la-decouverte.md`. Ce qui sera validé à l'écran
est donc exactement ce qui atterrira sur `main`.

### 2. Contrôle de non-régression §4 repassé

`generer_index.py` relancé : `INDEX.json` **inchangé** (3 entrées,
`echec-a-la-decouverte` démos=1, `mauvais-fou` démos=5, `nataf` démos=0).
Le second contrôle du §4 — rejeu du lecteur hors navigateur sur les trois
entrées — n'a pas été repassé ici : il l'avait été le 2026-08-09, et la
fusion n'a touché aucun de ses fichiers d'entrée.

### 3. Serveur de validation

`serveur_echecs.py` lancé en arrière-plan (`nohup`), port 8000.
`/`, `/Papu_Chess.html`, `INDEX.json` et `echec-a-la-decouverte.md`
répondent tous 200. URL de validation :
`http://localhost:8000/Papu_Chess.html` → 📚 Encyclopédie → onglet
Tactique → « L'échec à la découverte » → ▶️ Dérouler.

### 4. Aucune fusion vers `main`

`main` n'a bougé que par avance rapide sur son propre distant. Rien de la
branche n'est parti vers `main`.

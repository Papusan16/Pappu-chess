# 2026-09-10

## 07:35 — Reprise : le chantier chesscom est fusionné, REPRISE.md est périmé

Protocole de reprise exécuté : `REPRISE.md`, `_fonds/RESTE_A_FAIRE.md`,
`PRINCIPES.md`, dernier `_sessions/` (2026-09-05, rectifié le 09-06), plus
l'état réel côté git.

### Où on en est

**Le chantier `sauvegarde-chesscom` est terminé — Flavien a cliqué.**
`origin/main` est à `5e66b79`, *Merge pull request #6 from
Papusan16/wip-sauvegarde-chesscom-pr*, daté du **2026-09-09 07:20**. La
branche `wip-sauvegarde-chesscom-pr` (`4e96dee`) est à zéro commit
d'avance sur `origin/main` : tout est fusionné, rien ne traîne.

Les deux blobs premium sont bien archivés, mais **dans l'autre clone** —
`~/GoogleDrive2/Echecs/Papu-Chess/_sauvegardes/chesscom/` :
`papu_san_chesscom_data.json` = 179 920 o et `.csv` = 63 015 o, exactement
les octets inscrits au manifeste. Dans le clone
`~/Téléchargements/Echecs/Papu-Chess/`, le dossier ne contient que le
`MANIFESTE.md` — normal, les blobs sont gitignorés et ne voyagent pas par
git. C'est le clone Drive qui porte la sauvegarde, avec le bisync derrière.

### Ce qui reste ouvert

1. **`REPRISE.md` ment maintenant** : sa section 3 dit encore « fusion
   vers `main` en attente du clic de Flavien ». C'est fait. À rectifier,
   c'est le fichier lu en premier à chaque reprise.
2. **Le clone local `main` est périmé** : `106b72d` ici contre `5e66b79`
   sur origin. Les règles `deny` interdisent `git checkout main` à Claude
   Code — geste de Flavien, ou passage par une branche.
3. **Chantier réel en cours : la démonstration jouable**, branche
   `wip-demo-jouable` — FORMAT v7 (règles F8/F9/F10), lecteur d'étapes
   ramifié, overlay desktop. Il attend **la validation à l'écran**, pas du
   code.
4. **Ruleset GitHub `protection-main`** : toujours pas posé côté serveur.
   Les `deny` et le hook `pre-push` restent locaux et contournables.
   Marche à suivre dans `_sessions/2026-08-06.md`.
5. **Décalage non corrigé** : `RESTE_A_FAIRE.md` parle encore d'un
   « lecteur d'étapes à construire » et d'une migration « vers v3 », alors
   que `JOURNAL.md` acte le lecteur ramifié fusionné. À trancher par
   Flavien.
6. **Hors Papu-Chess — `gdrive-sync`** : l'étape 7 n'a jamais été faite.
   Aucune unité systemd, aucun timer : rien ne se synchronise tout seul,
   chaque run est manuel. Restent aussi la purge du `.git` obsolète sur
   `gdrive2:Echecs/Papu-Chess/.git`, les quatre originaux dans
   `pre-resync-backup/`, et le `client_secret_*.json` encore dans le
   périmètre synchronisé.

Proposition : rectifier `REPRISE.md` d'abord (court, et ça évite qu'une
prochaine reprise reparte sur une fausse piste), puis prendre
`wip-demo-jouable` pour la validation à l'écran.

---

## 07:55 — AUDIT DE VÉRACITÉ des assertions de reprise (lecture seule)

**Note d'exécution.** Le répertoire de travail demandé était
`/home/lui/GoogleDrive2/Echecs/Papu-Chess`. L'audit y a bien lu (points 1,
5 à 10), mais **la restitution est écrite dans le clone
`~/Téléchargements/Echecs/Papu-Chess`, branche `wip-reprise-2026-09-10`** :
c'est le seul des deux clones qui porte l'entrée de 07:35 à ne pas
réécrire. Le clone GoogleDrive2 est resté sur `wip-sauvegarde-chesscom`
(`6b46725`), n'a pas le commit de fusion, et n'a pas de fichier
`_sessions/2026-09-10.md` ; aucun `git checkout` n'y a été fait.

**Bilan : 3 verdicts FAUX (points 6, 9, 10), 7 VRAI, 0 INVÉRIFIABLE.**
Les trois portent tous sur `gdrive-sync`, aucun sur Papu-Chess.

### A. Assertions que seul le local peut trancher

**1. Les deux blobs premium, aux octets du manifeste — VRAI.**

```
$ ls -l ~/GoogleDrive2/Echecs/Papu-Chess/_sauvegardes/chesscom/
-rw-rw-r-- 1 lui lui   1021 sept.  6 07:10 MANIFESTE.md
-rw-rw-r-- 1 lui lui  63015 sept.  6 07:09 papu_san_chesscom_data.csv
-rw-rw-r-- 1 lui lui 179920 sept.  6 07:09 papu_san_chesscom_data.json
$ wc -c papu_san_chesscom_data.json papu_san_chesscom_data.csv
179920 papu_san_chesscom_data.json
 63015 papu_san_chesscom_data.csv
```

Manifeste : « `papu_san_chesscom_data.json` : 179 920 o ;
`papu_san_chesscom_data.csv` : 63 015 o ». Concordance exacte, à l'octet.

**2. Le clone Téléchargements ne contient que MANIFESTE.md — VRAI.**

```
$ ls -la ~/Téléchargements/Echecs/Papu-Chess/_sauvegardes/chesscom/
total 12
drwxrwxr-x 2 lui lui 4096 sept.  9 07:07 .
drwxrwxr-x 3 lui lui 4096 sept.  9 07:07 ..
-rw-rw-r-- 1 lui lui 1021 sept.  9 07:07 MANIFESTE.md
```

`ls -la` : aucun fichier caché non plus.

**3. `main` local à 106b72d contre 5e66b79 sur origin — VRAI. Retard : 12 commits.**

```
$ git rev-parse main
106b72d83ba52f5d6a5b8d58b8daff897f584cec
$ git rev-parse origin/main
5e66b790cea6214bb66c2eb24f9494596a39d8fe
$ git rev-list --count main..origin/main
12
$ git rev-list --count origin/main..main
0
```

Retard pur : `main` local est un ancêtre strict de `origin/main`, aucune
divergence.

**4. Les règles `deny` interdisent `git checkout main` — VRAI.**

```
$ grep -n "checkout main\|switch main" .claude/settings.json
8:      "Bash(git checkout main)",
9:      "Bash(git checkout main *)",
10:      "Bash(git switch main)",
11:      "Bash(git switch main *)",
```

### B. Assertions sur gdrive-sync

**5. bisync bidirectionnel, pas une copie à sens unique — VRAI.**

La ligne qui tranche, `~/.local/lib/gdrive-sync/gdsync/runner.py:64`, dans
`build_command()` :

```python
    cmd = [
        exe, "bisync", str(pair.local), pair.remote_spec,
        "--workdir", str(paths.workdir(cfg.state_dir, pair.name)),
        "--check-access",
        "--max-delete", str(cfg.max_delete),
        "--conflict-resolve", "none",
        "--backup-dir1", archive_local,
        "--backup-dir2", archive_remote,
```

Le sous-commande est `bisync` — bidirectionnel par construction. Aucun
`copy`, `copyto` ni `sync` unidirectionnel dans le moteur ; `grep -n
"bisync\|copyto\|rclone"` sur `runner.py` ne rend que des occurrences de
`bisync`. Le module porte d'ailleurs en tête : « Ce module ne contient
volontairement AUCUN drapeau de resynchronisation : seul l'exécutable
gdrive-sync-resync en injecte un ». Le fichier intégral fait 8 595 o et
n'est pas recopié ici ; chemin :
`~/.local/lib/gdrive-sync/gdsync/runner.py`.

**MAIS — et c'est le point 6 — ce moteur n'est pas le seul à tourner.**

**6. « Aucune unité systemd, aucun timer, rien ne tourne automatiquement » — FAUX.**

La moitié systemd de l'assertion est vraie :

```
$ systemctl --user list-unit-files | grep -i gdrive
(aucune sortie, code 1)
$ ls -la ~/.config/systemd/user/
drwxr-xr-x 2 lui lui 4096 mars  14 11:43 default.target.wants
drwxr-xr-x 2 lui lui 4096 mars  14 11:42 timers.target.wants
$ systemctl --user list-timers --all
NEXT                          LAST                          UNIT
Thu 2026-09-10 08:11:18 CEST  Wed 2026-09-09 08:11:18 CEST  launchpadlib-cache-clean.timer
Thu 2026-09-10 09:00:00 CEST  Thu 2026-09-10 06:00:31 CEST  snap.firmware-updater.firmware-notifier.timer
2 timers listed.
```

Aucune unité, aucun timer `gdrive`. **Mais la conclusion « rien ne tourne
automatiquement » est fausse : deux tâches cron tournent toutes les 10
minutes.**

```
$ crontab -l
*/10 * * * * /home/lui/rclone_sync_bidirectionnel.sh
*/10 * * * * /home/lui/rclone_sync_bidirectionnel_2.sh
$ ls -l ~/rclone_sync_bidirectionnel*.sh
-rwx------ 1 lui lui 676 août  31 06:16 /home/lui/rclone_sync_bidirectionnel.sh
-rwx------ 1 lui lui 680 août  31 06:16 /home/lui/rclone_sync_bidirectionnel_2.sh
```

Contenu du premier (le second est identique, `gdrive2:` /
`$HOME/GoogleDrive2` / `bisync2.log`) :

```bash
#!/bin/bash
export RCLONE_CONFIG_PASS="********"      # ← EN CLAIR dans le fichier
export DISPLAY=:0
export DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$(id -u)/bus"

REMOTE="gdrive:"
LOCAL="$HOME/GoogleDrive"
LOGFILE="$HOME/.cache/rclone/bisync.log"

mkdir -p "$LOCAL"
mkdir -p "$(dirname "$LOGFILE")"

rclone bisync "$REMOTE" "$LOCAL" \
  --create-empty-src-dirs \
  --compare size,modtime,checksum \
  --conflict-resolve newer \
  --log-file="$LOGFILE" \
  --log-level INFO

BYTES_LINE=$(tail -30 "$LOGFILE" | grep "Transferred:" | tail -1)

if [[ "$BYTES_LINE" != *"0 B / 0 B"* ]]; then
    notify-send "Synchro GoogleDrive" "${BYTES_LINE#*Transferred:}" -i emblem-synchronizing 2>/dev/null
fi
```

Trois constats qui découlent de ce script et qu'aucun fichier du dépôt ne
dit :

- **La passphrase de la config rclone y est écrite en clair.** Le fichier
  est en `700`, mais la doctrine de `gdrive-sync` (« la passphrase est dans
  le trousseau, jamais en argument de commande ») est contournée par ce
  vestige. C'est le point le plus sérieux de l'audit.
- **Ces scripts n'ont AUCUNE des protections du moteur** : ni
  `--check-access`, ni `--max-delete`, ni `--backup-dir`, et
  `--conflict-resolve newer` au lieu de `none`. C'est exactement le
  garde-fou qui a sauvé les données le 2026-09-08 lors de l'inversion des
  comptes.
- **Ils n'excluent pas `**/.git/**`**, là où `config.toml` l'exclut
  explicitement (« un passage périodique peut en capturer un état
  incohérent et corrompre le dépôt »).

**7. Un `.git` obsolète subsiste sur `gdrive2:Echecs/Papu-Chess/.git` — VRAI.**

```
$ rclone size gdrive2:Echecs/Papu-Chess/.git
Total objects: 844
Total size: 19.247 MiB (20181832 Byte)
$ rclone lsl gdrive2:Echecs/Papu-Chess/.git | (dates min/max)
2026-07-16 07:20:58.801000000
2026-08-12 11:39:37.645000000
```

844 objets, 20 181 832 octets — exactement les chiffres annoncés. Figé : le
plus récent date du **2026-08-12**, soit le jour où `**/.git/**` est
devenu une exclusion. Il ne bouge plus, et il ne bougera plus.

**8. `pre-resync-backup/` contient quatre originaux — VRAI.**

```
$ ls -la ~/.local/state/gdrive-sync/pre-resync-backup/
-rw-rw-r-- 1 lui lui    7448 mars  18  2025 123.xlsx
-rw-rw-r-- 1 lui lui  821155 oct.  10  2018 2007_11_ALINEA.pdf
-rw-rw-r-- 1 lui lui   30684 mai   12 08:05 Fiche_Institutionnelle_1_Cadre_PJJ_2026.docx
-rw-rw-r-- 1 lui lui 1271221 mai   11 16:02 Manuel_de_Mathématiques.docx
$ du -sh
2,1M
```

Quatre fichiers, 2,1 Mo, dans `~/.local/state/gdrive-sync/pre-resync-backup/`.

**9. « Un `client_secret_*.json` traîne dans `~/GoogleDrive/` » — FAUX.**

```
$ find /home/lui -name "client_secret*" 2>/dev/null
/home/lui/.config/gdrive-sync/client_secret_1063531291154-ugni7bqnquv6tddaqaq3b3i7de5necuj.apps.googleusercontent.com.json
-rw------- 1 lui lui 410 août  29 07:08
```

**Un seul exemplaire sur toute la machine, et il n'est plus dans le
périmètre synchronisé** : `~/.config/gdrive-sync/` n'est ni
`~/GoogleDrive` ni `~/GoogleDrive2`. Permissions `600`. Contenu non
affiché. Aucune exclusion `gdrive-sync` ni règle `.gitignore` ne le
concerne — elles sont devenues sans objet. **Cet item de la liste
« reste en attente » est donc à fermer.**

**10. « gdrive-sync a tourné le 09/09 » — FAUX.**

Le moteur `gdrive-sync` n'a **rien** exécuté le 09/09. Dernière ligne de
`~/.local/state/gdrive-sync/history.jsonl` :

```json
{"ts": "2026-09-08T10:21:14+02:00", "pair": "gdrive2", "result": "ok", "duration_s": 126.2,
 "transferred": 0, "deleted": 0, "errors": 0, "exit_code": 0, "log": "20260908-101908.log", "action": "run"}
```

Le journal du moteur s'arrête au **2026-09-08 10:21**, sur les deux runs
de contrôle à zéro transfert / zéro suppression déjà consignés.

**En revanche le cron a tourné le 09/09 — 282 fois, et a échoué 282 fois.**

```
$ grep -c '^2026/09/09.*Bisync aborted' ~/.cache/rclone/bisync.log   → 141
$ grep -c '^2026/09/09.*Bisync successful' ~/.cache/rclone/bisync.log → 0
$ grep -c '^2026/09/09.*Bisync aborted' ~/.cache/rclone/bisync2.log  → 141
$ grep -c '^2026/09/09.*Bisync successful' ~/.cache/rclone/bisync2.log → 0
premier : 2026/09/09 00:00:14 ERROR : Bisync aborted. Must run --resync to recover.
dernier : 2026/09/09 23:50:02 ERROR : Bisync aborted. Must run --resync to recover.
```

Motif, identique à chaque passage :

```
2026/09/10 06:50:01 INFO  : Synching Path1 "gdrive:/" with Path2 "/home/lui/GoogleDrive/"
2026/09/10 06:50:01 ERROR : Bisync critical error: cannot find prior Path1 or Path2 listings,
                            likely due to critical error on prior run
Path1: /home/lui/.cache/rclone/bisync/gdrive_..home_lui_GoogleDrive.path1.lst
Path2: /home/lui/.cache/rclone/bisync/gdrive_..home_lui_GoogleDrive.path2.lst
2026/09/10 06:50:01 ERROR : Bisync aborted. Must run --resync to recover.
Errors:                 1 (fatal error encountered)
2026/09/10 06:50:01 NOTICE: Failed to bisync: bisync aborted
```

**Le cron cherche son état dans `~/.cache/rclone/bisync/`, tandis que le
moteur écrit le sien dans `~/.local/state/gdrive-sync/`.** Les deux
systèmes ne partagent pas de `workdir` : le resync fait le 08/09 par
`gdrive-sync` n'a jamais réparé le cron.

**Dernier succès réel du cron** :

```
$ grep "Bisync successful" ~/.cache/rclone/bisync.log  | tail -1
30886:2026/09/01 03:44:36 INFO  : Bisync successful
$ grep "Bisync successful" ~/.cache/rclone/bisync2.log | tail -1
9893:2026/08/31 08:13:59 INFO  : Bisync successful
```

Le cron `gdrive` est mort le **2026-09-01 à 03:44**, le cron `gdrive2` le
**2026-08-31 à 08:13**. Depuis, ~144 échecs par jour et par paire,
**silencieux** : le `notify-send` du script ne se déclenche que si des
octets ont été transférés, or un run avorté transfère `0 B / 0 B`. Les
deux logs pèsent 4,6 Mo chacun et ne contiennent plus que des échecs.

Le dernier passage tracé, à l'instant de l'audit :

```
$ grep "rclone_sync" /var/log/syslog | tail -3
2026-09-10T06:50:01 CRON[43639]: (lui) CMD (/home/lui/rclone_sync_bidirectionnel_2.sh)
2026-09-10T07:00:01 CRON[44146]: (lui) CMD (/home/lui/rclone_sync_bidirectionnel.sh)
2026-09-10T07:00:01 CRON[44147]: (lui) CMD (/home/lui/rclone_sync_bidirectionnel_2.sh)
```

**Conséquence pour Papu-Chess.** Le manifeste
`_sauvegardes/chesscom/MANIFESTE.md` affirme : « Non versionnés (backup
via Google Drive) ». Les deux blobs sont bien dans `~/GoogleDrive2/`, mais
**rien ne les a poussés vers Drive depuis le 2026-09-08 10:21** — le seul
mécanisme qui fonctionne encore est manuel, et le seul mécanisme
automatique est en panne depuis dix jours. La sauvegarde annoncée n'est
pas prouvée. **À vérifier avant de considérer les blobs comme sauvegardés
hors machine.** (Non vérifié par cet audit : leur présence effective côté
`gdrive2:`, qui demanderait un `rclone lsl` sur la destination.)

### C. Qualité de tenue du journal

**11. Les entrées datées de `JOURNAL.md` sur `origin/main` :**

```
205:## 2026-08-01 → 08-03 — Encyclopédie : FORMAT v3, lecteur ramifié, overlay porte unique
260:## 2026-08-04 — PALIER : la branche lecteur-etapes-ramifie fusionnée dans main
280:## 2026-09-05 — Consignation de la capture premium Chess.com du 04/09
301:## 2026-09-06 — Le détail premium rentre dans le rang : `_rapports/` redevient transient
```

Quatre entrées datées seulement, plus une section « Fait récemment » en
tête, **non datée**, en liste de puces — et une section « Prochains
chantiers » en queue. **Entre le 04/08 et le 05/09, le journal de `main`
ne dit rien : un mois de blanc.**

**12. Confrontation à l'historique git réel (`--all`, depuis le 31/08) :**

```
2026-09-10 06:47:44 d2e10c2 _sessions : reprise du 2026-09-10 (chesscom fusionne, REPRISE.md perime)
2026-09-09 07:20:14 5e66b79 Merge pull request #6 from Papusan16/wip-sauvegarde-chesscom-pr
2026-09-09 07:07:49 4e96dee REPRISE : le chantier sauvegarde-chesscom réapparaît en attente de fusion
2026-09-06 07:10:38 6b46725 Cloture chesscom : bon export (jeu 13:03, +1 en differe), manifeste octets exacts + schema
2026-09-06 04:44:35 15eb0e7 JOURNAL : la branche parasite n'est supprimee qu'en local
2026-09-06 04:32:28 34538db JOURNAL : entree 2026-09-06 (migration vers _sessions, branche parasite)
2026-09-06 04:31:06 6273612 Migre le detail premium _rapports -> _sessions ; repointe le manifeste
2026-09-06 04:10:56 7346e84 REPRISE : parque la copie des blobs premium, avec ses deux portes
2026-09-06 04:05:58 cfceaec Rectifie le bilan En differe : 14 classees + 13 amicales
2026-09-05 11:09:40 c7a6a96 Consigne la sauvegarde des donnees premium Chess.com (capture 04/09)
2026-09-01 09:11:10 9f81cf6 Étape 6 : deux scories de rédaction ; le compte de contrôles daté
2026-09-01 04:06:27 ef43016 JOURNAL + _sessions : wip-demo-jouable resynchronisée sur main
2026-09-01 04:01:55 30c2d21 Synchronise wip-demo-jouable avec main
2026-08-31 10:56:21 332c70f Mauvais fou : le trait forcé neutralise le champ en-passant
2026-08-31 09:32:41 0d75913 Journal du 31 août : vérification du dépôt, publication de digue-hook-pre-push
```

**(a) Gestes présents dans git, absents du `JOURNAL.md` de `main` :**

- **2026-08-31** (`0d75913`, `332c70f`) — le trait forcé et l'en-passant du
  mauvais fou. L'entrée existe, mais **uniquement sur
  `origin/wip-fonds-couleurs`** (`## 2026-08-31 — Mauvais fou : le trait
  forcé neutralise l'en-passant`). Invisible depuis `main`.
- **2026-09-01** (`30c2d21`, `ef43016`, `9f81cf6`) — resynchronisation de
  `wip-demo-jouable` et validation visuelle du tronc. Deux entrées
  existent, **uniquement sur `origin/wip-demo-jouable`** (`## 2026-09-01`
  et `## 2026-09-01 (2) — Validation visuelle du tronc : conforme, trois
  défauts de rendu isolés`). Invisibles depuis `main`.
- **2026-09-09** (`4e96dee`, `5e66b79`) — **aucune entrée nulle part.** Le
  geste le plus structurant de la période, la fusion de la PR #6 sur
  `main`, n'est journalisé par personne. Le dépôt ne sait pas qu'il a
  fusionné.
- **2026-09-10** (`d2e10c2`) — consigné dans `_sessions/`, **pas dans
  `JOURNAL.md`** (comblé par l'entrée écrite en même temps que cet audit).
- **2026-08-10 / 08-11 / 08-12** — dix commits (fonds de case, menace,
  pile d'historique encyclopédie, `encyChainTo()`, l'étreinte à
  retardement, capitalisation LuisJabierr–Papu_san). **Aucune entrée de
  `JOURNAL.md` sur aucune branche, aucun fichier `_sessions/`.**

**(b) Entrées du journal ne correspondant à aucun commit :** aucune. Les
quatre entrées datées de `main` s'adossent toutes à des commits réels.

**(c) Entrées dont la date ne colle pas :** aucun écart. Les commits du
06/09 portent `+0000` (`04:05` à `04:44` UTC = `06:05` à `06:46` CEST) et
tombent bien le 6 septembre en heure locale ; l'entrée `## 2026-09-06` est
juste. La bascule de fuseau entre `+0000` (sessions distantes) et `+0200`
(sessions locales) n'a produit aucune date fausse.

**Le vrai défaut n'est donc pas la fausseté, c'est la dispersion :** le
journal est réparti sur quatre branches qui ne se voient pas, et `main` —
la seule branche qu'une reprise à froid lit — en porte la version la plus
pauvre.

**13. Jours de travail effectif sans fichier `_sessions/` :**

Jours portant au moins un commit (toutes branches, depuis le 30/07) :

```
07-30  08-01  08-03  08-04  08-05  08-06  08-07  08-08  08-09
08-10  08-11  08-12  08-31  09-01  09-05  09-06  09-09  09-10
```

Union des fichiers `_sessions/` sur toutes les branches :

```
2026-07-30  2026-08-01  2026-08-03  2026-08-04  2026-08-05
2026-08-05-reco-lichess   2026-08-06   2026-08-06-reco-protocole-reprise
2026-08-07  2026-08-08  2026-08-09  2026-08-09-reco-coachs
2026-08-31  2026-09-01  2026-09-05  2026-09-10  README
```

**Cinq jours travaillés sans aucun fichier de session, sur aucune
branche : 2026-08-10, 2026-08-11, 2026-08-12, 2026-09-06, 2026-09-09.**
Le 09-06 a une entrée de `JOURNAL.md` mais pas de `_sessions/` ; les
quatre autres n'ont rien.

**Et un manque qui compte davantage — `_sessions/` sur `main` :**

```
$ git ls-tree --name-only origin/main _sessions/
_sessions/2026-07-30.md  _sessions/2026-08-01.md  _sessions/2026-08-03.md
_sessions/2026-08-04.md  _sessions/2026-08-05.md  _sessions/2026-09-05.md
_sessions/README.md
```

**`main` ne porte que 6 fichiers de session sur les 15 qui existent.**
Manquent notamment `2026-08-06`, `08-07`, `08-08`, `08-09`, `08-31`,
`09-01`. Conséquence directe et vérifiable : **`REPRISE.md`, sur `main`,
renvoie à `_sessions/2026-08-06.md` pour la marche à suivre du ruleset
`protection-main` — un fichier qui n'existe pas sur `main`.** Le pointeur
le plus important du fichier lu en premier à chaque reprise pointe dans le
vide. Le fichier existe, sur `origin/wip-demo-jouable`.

### Ce que l'audit change à l'entrée de 07:35

Rien n'y est faux. Deux points s'y ajoutent :

- L'item 6 (« gdrive-sync : rien ne tourne automatiquement ») était vrai
  du moteur et faux de la machine. **Deux crons vestigiaux tournent toutes
  les 10 minutes et échouent depuis le 31/08 et le 01/09**, sans aucun des
  garde-fous du moteur, avec la passphrase rclone en clair.
- L'item sur le `client_secret_*.json` est **à fermer** : il n'est plus
  dans le périmètre synchronisé.

Et une chose que 07:35 ne disait pas : **la sauvegarde Drive des blobs
premium n'est pas prouvée** — le seul mécanisme automatique est en panne
depuis le 08/09.

**Aucun fichier modifié hors de ce fichier et de `JOURNAL.md`. Rien
installé, rien réparé, aucune commande d'écriture lancée.**

---

## 2026-09-15, 07:20 — Neutralisation du cron vestigial ; la contradiction du 07/09 levée (c'est l'audit qui avait tort)

**Note de date.** Cette entrée est écrite le **15 septembre**, pas le 10 :
la consigne demandait l'append dans `_sessions/2026-09-10.md` et les
suffixes `.desactive-2026-09-10`, tous deux respectés à la lettre. Les
noms de fichiers portent donc une date qui n'est pas celle du geste.

### 1. Sauvegarde du crontab — faite

`~/.local/state/gdrive-sync/crontab-avant-2026-09-10.txt`, 109 octets :

```
(ligne vide)
*/10 * * * * /home/lui/rclone_sync_bidirectionnel.sh
*/10 * * * * /home/lui/rclone_sync_bidirectionnel_2.sh
```

### 2. Les deux lignes retirées — faite

Le crontab ne contenait **rien d'autre** que ces deux lignes (plus une
ligne vide en tête). Après retrait :

```
$ crontab -l
(ligne vide, rc=0)
```

Aucune autre ligne à préserver — il n'y en avait pas. Le crontab installé
est conservé en clair dans
`~/.local/state/gdrive-sync/crontab-nouveau.txt`.

*Détail d'exécution* : `crontab <fichier>` tronque les chemins longs — il
a refusé le fichier du répertoire scratch (« Aucun fichier ou dossier de
ce nom », chemin coupé à `…/scratchpad/c`). Passé par un chemin court sous
`~/.local/state/gdrive-sync/`.

### 3. Scripts renommés — faite

```
'/home/lui/rclone_sync_bidirectionnel.sh'   -> '…sh.desactive-2026-09-10'
'/home/lui/rclone_sync_bidirectionnel_2.sh' -> '…sh.desactive-2026-09-10'
-rwx------ 1 lui lui 676 août 31 06:16 rclone_sync_bidirectionnel.sh.desactive-2026-09-10
-rwx------ 1 lui lui 680 août 31 06:16 rclone_sync_bidirectionnel_2.sh.desactive-2026-09-10
```

Contenu intact, mode `700` conservé, rien supprimé.

### 4. La passphrase — où elle est, sans l'afficher

**Trois fichiers, aucun sur Drive, aucun suivi par git.**

| Fichier | Ligne(s) | Mode | Sur Drive ? | git ? |
|---|---|---|---|---|
| `~/rclone_sync_bidirectionnel.sh.desactive-2026-09-10` | 2 | `700` | non | hors dépôt |
| `~/rclone_sync_bidirectionnel_2.sh.desactive-2026-09-10` | 2 | `700` | non | hors dépôt |
| `~/.bash_history` | **19 occurrences** — lignes 84, 111, 391, 411, 438, 457, 475, 483, 502, 520, 533, 552, 571, 590, 633, 642, 669, 711, 746 | `600` | non | hors dépôt |

- **Ni `/home/lui/GoogleDrive` ni `/home/lui/GoogleDrive2` ne la
  contiennent** (`grep -rlIF` sur chacun : aucun résultat). Elle n'a donc
  **pas** été recopiée sur Drive.
- **Aucun des trois n'est dans un dépôt git** — `~` n'est pas un dépôt.
  La question « suivi ou untracked » ne se pose pas : ils sont hors
  périmètre.
- Les deux logs du cron (`~/.cache/rclone/bisync*.log`) ne la contiennent
  pas : elle passait par l'environnement, pas par la ligne de commande.
- **`~/.bash_history` est la vraie surprise** : 19 occurrences. Elles
  viennent de commandes tapées à la main, pas des scripts.

**Portée du balayage — honnêteté sur ce qui n'a pas été couvert.** Le
`grep -rlIF` sur `/home/lui` entier a été tué au bout de 9 minutes sans
finir. Ont été couverts de façon concluante : la racine du home,
`.bashrc`, `.profile`, `.bash_profile`, `.config`, `.local/bin`,
`.local/lib`, `.local/state`, `.ssh`, les historiques shell,
`GoogleDrive`, `GoogleDrive2`, `Téléchargements`, `Documents`, `Bureau`,
`Images`, `Vidéos`, `Musique`. **`~/.cache` n'a pas pu être balayé
entièrement** (délai dépassé) — seuls les deux logs rclone y ont été
vérifiés, négatifs. Le résultat est donc solide mais **pas exhaustif**.

**Incident à signaler.** Un `pgrep -af` lancé pour vérifier l'état du
balayage a **affiché la passphrase en clair dans la sortie du terminal** :
elle figurait dans la ligne de commande du `grep` en cours. La consigne
« sans l'afficher » a été tenue partout ailleurs, mais **elle est dans le
défilement du terminal de cette session**. Raison de plus de la changer,
et de vider le scrollback.

#### Marche à suivre pour la changer (geste de Flavien)

La config est chiffrée : `/home/lui/.config/rclone/rclone.conf`, mode
`600`, première ligne `# Encrypted rclone configuration File`.

**a. Changer la passphrase de la config rclone**

```
rclone config
  → s) Set configuration password
  → c) Change Password          (demande l'ancienne, puis la nouvelle deux fois)
  → q) Quit to main menu
  → q) Quit config
```

Ne pas choisir `u) Unencrypt configuration` : cela écrirait les jetons
OAuth des deux comptes en clair sur le disque.

**b. La déposer au trousseau, comme le fait le moteur**

```
gdrive-sync set-password
```

C'est la commande prévue (`gdsync/secret.py`, `store_in_keyring`) : elle
écrit sous le label `gdrive-sync/rclone-config` avec `replace=True`, donc
elle écrase proprement l'ancienne valeur. **`secret-tool` n'est pas
installé sur cette machine** — c'est le module python `secretstorage` qui
fait le travail ; inutile de chercher une commande `secret-tool`. Le
trousseau doit être déverrouillé (session graphique ouverte) : le module
refuse par principe de demander le déverrouillage, pour ne pas bloquer sur
une boîte de dialogue invisible.

**c. Vérifier**

```
gdrive-sync check        # valide la configuration et l'accès au secret
```

**d. Purger les trois fichiers**

Les deux scripts désactivés : y remplacer la ligne 2 par un commentaire,
ou les supprimer une fois les chemins et noms de remotes relevés.
`~/.bash_history` : les 19 lignes sont à retirer (`history -d`, ou édition
directe après `history -w`), sachant que le shell en cours réécrira le
fichier à sa fermeture.

**La passphrase n'a pas été changée par Claude Code — c'est le geste de
Flavien, comme demandé.**

### 5. Aucun autre déclencheur automatique

```
$ crontab -l                       → vide
$ systemctl --user list-timers     → 2 timers : launchpadlib-cache-clean,
                                      snap.firmware-updater.firmware-notifier
                                      (aucun lien avec gdrive/rclone)
$ ls -la /etc/cron.d/              → anacron, e2scrub_all, sysstat, .placeholder
$ ls -la ~/.config/autostart/      → vide
$ grep -rl "rclone\|gdrive" /etc/cron.d/ /etc/crontab /etc/cron.hourly/ /etc/cron.daily/
                                   → aucun
$ systemctl list-unit-files | grep -i "gdrive\|rclone"
                                   → aucun
```

**Plus rien ne déclenche de synchronisation automatiquement.** À noter :
`config.toml` porte `autostart = true` pour l'interface de barre des
tâches, mais `~/.config/autostart/` est vide — le `gdrive-sync-tray` ne
démarre pas non plus tout seul.

### 6. La contradiction du 07/09 — levée. L'audit avait tort.

**Il n'y a pas de contradiction, et l'erreur est de mon côté.**

Le fichier lu est bien
**`~/.local/state/gdrive-sync/history.jsonl`** (2 735 o, mode 600, mtime
2026-09-08 10:21). Il couvre **les deux destinations** — chaque ligne
porte un champ `pair`, avec des entrées `gdrive` et `gdrive2`. Et il
**contient cinq entrées du 07/09**, dont celle-ci :

```
2026-09-07T06:48:40+02:00  gdrive2  run  ok  log=20260907-063855.log
```

**Le nom du log — `20260907-063855` — est exactement celui du dossier
`.gdrive-sync-archive/20260907-063855` observé sur le Drive.** Le
`run_stamp` sert à la fois de nom de log et de nom de dossier d'archive
(`build_command()` : `--backup-dir2 pair.remote_sub(ARCHIVE_DIRNAME,
run_stamp)`). La trace Drive et le journal disent la même chose.

Le second journal existe, et il est formel —
`~/.local/state/gdrive-sync/pairs/gdrive2/logs/20260907-063855.log`,
12 297 o :

```
06:46:54 INFO : - Path1  File is new  - Echecs/Papu-Chess/_sauvegardes/chesscom/papu_san_chesscom_data.json
06:46:54 INFO : - Path1  Queue copy to Path2 - gdrive2:/Echecs/…/papu_san_chesscom_data.json
06:47:37 INFO : Echecs/Papu-Chess/_sauvegardes/chesscom/MANIFESTE.md: Copied (new)
06:47:37 INFO : Echecs/Papu-Chess/_sauvegardes/chesscom/papu_san_chesscom_data.csv: Copied (new)
06:47:38 INFO : Echecs/Papu-Chess/_sauvegardes/chesscom/papu_san_chesscom_data.json: Copied (new)
06:46:54 INFO : Path1:   20 changes:    4 new,    9 modified,    7 deleted
06:48:40 INFO : Bisync successful
```

**Téléversement à 06:47:37-38 le 07/09, heure locale — exactement ce que
Flavien a vu sur le Drive.**

Contrôle direct de la destination, à l'instant :

```
$ rclone lsl gdrive2:Echecs/Papu-Chess/_sauvegardes/chesscom
     1021 2026-09-06 07:10:07  MANIFESTE.md
    63015 2026-09-06 07:09:56  papu_san_chesscom_data.csv
   179920 2026-09-06 07:09:55  papu_san_chesscom_data.json
```

**D'où venait le « 01/09 ».** Il n'a jamais été tiré de `history.jsonl`.
C'est le dernier `Bisync successful` de **`~/.cache/rclone/bisync.log`**,
le journal du **cron**, une tout autre source. L'audit du 10/09 donnait
correctement les deux dates séparément — dernier run du moteur au **08/09
10:21**, dernier succès du cron au **01/09 03:44** — mais en a tiré une
conclusion fausse, écrite noir sur blanc : « la sauvegarde annoncée n'est
pas prouvée ». **Elle l'était.** Le moteur avait poussé les deux blobs sur
Drive le 07/09 à 06:47, et ils y sont, aux octets exacts.

**Ce qui reste vrai** : depuis le 08/09 10:21, plus aucune synchronisation
automatique. Mais ces trois fichiers-là n'ont pas changé depuis le 06/09,
donc la copie Drive est à jour. **Le manifeste ne mentait pas.** La
correction est portée dans `JOURNAL.md`.

### Ce qui reste à faire

- **Changer la passphrase** (a→d ci-dessus) — geste de Flavien.
- **Étape 7 de `gdrive-sync`** : le moteur a une sous-commande
  `gdrive-sync install-units` (« génère les unités systemd, sans les
  activer »). C'est la voie propre pour retrouver une synchronisation
  automatique — avec `--check-access`, `--max-delete`, `--backup-dir` et
  l'exclusion `**/.git/**`, tout ce que le cron n'avait pas.
- Purge du `.git` obsolète sur `gdrive2:` (844 objets, 20 181 832 o) et
  des quatre originaux de `pre-resync-backup/` : inchangé.

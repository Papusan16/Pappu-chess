# 2026-09-10

## 07:35 — Reprise : le chantier chesscom est fusionné, REPRISE.md est périmé

Protocole de reprise exécuté : `REPRISE.md`, `_fonds/RESTE_A_FAIRE.md`,
`PRINCIPES.md`, dernier `_sessions/` (2026-09-05, rectifié le 09-06), plus
l'état réel côté git.

### Où on en est

**Le chantier `sauvegarde-chesscom` est terminé — Flavien a cliqué.**
`origin/main` est à `5e66b79`, *Merge pull request #6 from
Papusan16/wip-sauvegarde-chesscom-pr*, daté du **2026-09-09 07:20**. La
branche `wip-sauvegarde-chesscom-pr` (`4e96dee`) est à zéro commit
d'avance sur `origin/main` : tout est fusionné, rien ne traîne.

Les deux blobs premium sont bien archivés, mais **dans l'autre clone** —
`~/GoogleDrive2/Echecs/Papu-Chess/_sauvegardes/chesscom/` :
`papu_san_chesscom_data.json` = 179 920 o et `.csv` = 63 015 o, exactement
les octets inscrits au manifeste. Dans le clone
`~/Téléchargements/Echecs/Papu-Chess/`, le dossier ne contient que le
`MANIFESTE.md` — normal, les blobs sont gitignorés et ne voyagent pas par
git. C'est le clone Drive qui porte la sauvegarde, avec le bisync derrière.

### Ce qui reste ouvert

1. **`REPRISE.md` ment maintenant** : sa section 3 dit encore « fusion
   vers `main` en attente du clic de Flavien ». C'est fait. À rectifier,
   c'est le fichier lu en premier à chaque reprise.
2. **Le clone local `main` est périmé** : `106b72d` ici contre `5e66b79`
   sur origin. Les règles `deny` interdisent `git checkout main` à Claude
   Code — geste de Flavien, ou passage par une branche.
3. **Chantier réel en cours : la démonstration jouable**, branche
   `wip-demo-jouable` — FORMAT v7 (règles F8/F9/F10), lecteur d'étapes
   ramifié, overlay desktop. Il attend **la validation à l'écran**, pas du
   code.
4. **Ruleset GitHub `protection-main`** : toujours pas posé côté serveur.
   Les `deny` et le hook `pre-push` restent locaux et contournables.
   Marche à suivre dans `_sessions/2026-08-06.md`.
5. **Décalage non corrigé** : `RESTE_A_FAIRE.md` parle encore d'un
   « lecteur d'étapes à construire » et d'une migration « vers v3 », alors
   que `JOURNAL.md` acte le lecteur ramifié fusionné. À trancher par
   Flavien.
6. **Hors Papu-Chess — `gdrive-sync`** : l'étape 7 n'a jamais été faite.
   Aucune unité systemd, aucun timer : rien ne se synchronise tout seul,
   chaque run est manuel. Restent aussi la purge du `.git` obsolète sur
   `gdrive2:Echecs/Papu-Chess/.git`, les quatre originaux dans
   `pre-resync-backup/`, et le `client_secret_*.json` encore dans le
   périmètre synchronisé.

Proposition : rectifier `REPRISE.md` d'abord (court, et ça évite qu'une
prochaine reprise reparte sur une fausse piste), puis prendre
`wip-demo-jouable` pour la validation à l'écran.
